{
  "name": "my-bio-site",
  "version": "1.0.0",
  "description": "Personal bio site with Handlebars and Sass",
  "main": "build.js",
  "scripts": {
    "build": "node build.js && sass styles/style.scss dist/styles/style.css"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
