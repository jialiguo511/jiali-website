<div class="card mb-3">
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <h6 class="card-subtitle text-muted">{{company}} — {{date}}</h6>
    <p class="card-text mt-2">{{description}}</p>
  </div>
</div>
